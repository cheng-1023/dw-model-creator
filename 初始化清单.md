# 初始化清单

> 本清单盘点本次初始化创建的全部目录与文件，说明每项的状态，并给出投入使用前的必做事项。
> 样例数据仅用于演示格式，**正式使用前必须替换为经评审的真实内容**。

## 一、已创建的目录与文件

```text
dw-model-creator/
├── references/
│   ├── contracts/csv-schema.md        🔶 CSV 总纲（bootstrap 已定义，其余待定稿）
│   ├── runtime/                       ⬜ 空（递归求解/固定点/回流协议文档，后续编写）
│   ├── semantic-objects/              ⬜ 空（粒度/维度/度量/指标/标签方法，后续编写）
│   ├── model-design/
│   │   ├── conceptual/                ⬜ 空
│   │   ├── logical/                   ⬜ 空
│   │   └── physical/{dwd,dwm,dm}/     ⬜ 空（分层设计理念补齐前处理单元保持 reserved）
│   ├── asset-catalog/                 ⬜ 空（外置资产库接入方法，后续编写）
│   └── examples/                      ⬜ 空（优秀设计实践与反例，持续积累）
├── assets/
│   ├── bootstrap/                     ✅ 四件套 + manifest + README（checksum 为占位符！）
│   ├── knowledge/
│   │   ├── business-glossary/         ✅ 业务术语表样例（5 条）
│   │   └── word-roots/                ✅ 词根表样例（6 条）
│   ├── policies/
│   │   ├── dependency/                ✅ 依赖矩阵（8 条规则）+ 条件表
│   │   ├── naming/                    ✅ 命名规则样例（4 条）
│   │   ├── subject/                   ✅ 主题域样例（4 个）
│   │   └── layering/                  ✅ 分层规则样例（3 条）
│   ├── engine-profiles/postgresql-default/  ✅ Profile 三张表（元数据 1 条 + 类型映射 6 条 + 方言 5 条）
│   └── templates/                     ✅ 待确认模型 / 最终设计 / 检查报告 三个 Markdown 模板
├── scripts/
│   ├── validation/
│   │   ├── validate_csv_bundle.py     🔶 骨架（manifest/行数校验已实现，checksum 待实现）
│   │   └── validate_csv_control_plane.py  🔶 骨架（事件状态机已实现，部分一致性检查待实现）
│   ├── runtime/ knowledge/ asset-catalog/ ddl/  ⬜ 空
│   └── physical-design/{dwd,dwm,dm}/  ⬜ 空（reserved 占位）
├── evals/eval-001-贷款月报模型.md      ✅ 评估场景样例（混合粒度 + 复用判定）
└── tests/
    ├── fixtures/examples/asset-catalog/  ✅ 资产目录样例（4 张 CSV，脱敏 fixture）
    └── unit/ contract/ integration/      ⬜ 空
```

图例：✅ 样例已建可直接参考 ｜ 🔶 草案/骨架需完善 ｜ ⬜ 占位待编写

## 二、投入使用前的必做事项（按顺序）

1. **重算 bootstrap checksum**：完善 `scripts/validation/validate_csv_bundle.py` 的 checksum 复算逻辑，
   重算四件套的 `content_checksum` 与根记录 `bundle_checksum`，并把允许的
   `(bootstrap_version, expected_bundle_checksum)` 组合固定进校验器 `TRUSTED_BOOTSTRAP`。
2. **评审定稿策略**：依赖矩阵、命名、主题域、分层四套规则目前是样例值，
   必须由数仓治理团队评审替换为行内真实规范。
3. **补齐 CSV 总纲**：`references/contracts/csv-schema.md` 中"待定义"的运行期数据集
   （requests、capability_needs、bindings、三阶段模型表等 40+ 张）逐一定稿，并同步登记进 bootstrap 四件套。
4. **扩充 bootstrap 字段字典**：`field_dictionary.csv` 当前仅自描述两个数据集，
   需覆盖全部数据集；`bundle_manifest.csv` 行数同步更新。
5. **准备真实资产目录**：按 `tests/fixtures/examples/asset-catalog/` 四张表的格式导入现有存量模型；
   同时收集存量模型三阶段设计文档（仅有物理表不足以支持 reuse-as-is）。
6. **落实确认人与工作目录**：确定各阶段产物、物理模型确认的授权人/角色清单；
   指定外置工作目录（所有运行产物只写那里）。
7. **知识库积累**：业务术语表、词根表替换为行内真实术语资产，随 Skill 发布为版本化 CsvBundle。

## 三、试跑方式

```bash
# 骨架校验器试跑（当前校验 manifest 结构与行数，checksum 复算待实现）
python scripts/validation/validate_csv_bundle.py \
    --input-bundle assets/bootstrap \
    --work-root <你的外置工作目录>
```
