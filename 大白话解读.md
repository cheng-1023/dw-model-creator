# 大白话解读：dw-model-creator 到底在干什么？

> 这篇文章用来帮助理解 `README-model-design.md` 和 `workflow-model-design.md` 两份文档。
> 原文档是严谨的技术契约，本文是"翻译版"——用人话把核心思想讲清楚。两边说法冲突时，以原文档为准。

---

## 一句话版本

**这是一个"AI 帮你设计银行数据仓库模型"的工具：你告诉它业务需求，它产出三层设计文档和建表 SQL，每一步有据可查、有人把关，绝不瞎编。**

---

## 它解决什么痛点？

先看传统数仓建模的四个老大难：

| 痛点 | 现实场景 | 这个工具的办法 |
|---|---|---|
| 需求说不清 | 业务说"我要个月贷款报表"，字段口径全靠猜 | 把需求拆成一条条最小"能力项"，逐条落实 |
| 设计拍脑袋 | 表建完了，没人说得清某个字段是啥意思 | 每个字段、每个类型**必须有中文说明**，没说明直接不过关 |
| 层与层乱引用 | DM 的表直接引用另一张 DM 的表，绕过中间层，越绕越乱 | 一张"依赖矩阵"当交通法规：谁能用谁，白名单说了算 |
| 改了没人知道 | 模型偷偷改了一版，下游全炸了还不知道源头 | 所有设计都算"指纹"，内容变一丁点指纹就变，改动无处遁形 |

---

## 先搞懂三个分层：DWD / DWM / DM

把它想象成一个**中央厨房**：

```
DWD（明细层）── 净菜区：洗好、切好的原始食材（最细的明细数据）
     ↓
DWM（汇总层）── 半成品区：按菜配好的料（初步加工、聚合）
     ↓
DM（集市层）── 出餐区：直接端上桌的成品（面向使用的汇总表）
```

规矩：**只能从下往上一层层加工**。出餐区不能跳过半成品区直接去净菜区抓菜乱炒，更不能从别的出餐窗口端菜再转手（同层互相引用是禁止的）。

---

## 核心套路："反向定义、正向验证"

用文档里的贷款月报举例。需求方要两张数：

- 产品月末贷款余额（按 月份×机构×产品）
- 机构月末贷款余额（按 月份×机构）

**第一步：反向定义（从需求倒推要建什么表）**

```
需求方要"月末贷款余额"
  → DM 层缺一张月粒度汇总表？好，要建
    → 这张表的数据哪来？DWM 层得有汇总
      → DWM 的数据哪来？DWD 层得有明细流水
        → 到最底层了，反向推完，收工
```

就像从"客人要点什么菜"倒推出"要备什么食材"。

**第二步：正向验证（从底层往上验一遍能不能真做出来）**

```
DWD 明细表建好了
  → 用它真能算出 DWM 的汇总吗？粒度对得上吗？
    → DWM 能算出 DM 吗？缺维度吗？
      → DM 真能满足需求方要的每一个字段吗？
```

每张表、每个字段都能沿着这条链**追溯到源头**，谁也糊弄不了谁。

---

## 三个设计阶段 = 盖房子

每个模型都必须按顺序走三步，**不能跳级**：

| 阶段 | 类比 | 干什么 | 产出 |
|---|---|---|---|
| 概念模型 | 和业主聊天 | 搞清楚"有哪些人、哪些事、什么关系"，完全不碰数据库 | 业务实体、关系清单 |
| 逻辑模型 | 建筑图纸 | 画结构：表、字段、主键、粒度，但**不挑建材**（不管什么数据库） | 引擎无关的表设计 |
| 物理模型 | 施工图 | 具体到钢筋水泥：PostgreSQL 的字段类型、索引、分区、约束 | 可执行的表设计 |
| DDL | 开工单 | 施工图确认签字后，才打印 CREATE TABLE 的 SQL | .sql 文件 |

两个关键规矩：

1. **不能拿施工图冒充图纸**——就算你只想要一张物理表，也必须把概念、逻辑两步走完（不然又是一笔糊涂账）。
2. **签字确认才开工**——物理模型没人确认之前，绝不生成 DDL。改了设计？之前签的字作废，重新来。

---

## 文档里的"黑话"翻译表

| 原文术语 | 大白话 |
|---|---|
| CapabilityNeed（能力需求） | 把"我要什么"拆成的最小一条，比如"按月份×机构×产品的月末贷款余额" |
| grain（粒度） | 一行数据代表什么："一行 = 一个月 × 一个机构 × 一个产品"。粒度是建模里最要命的概念，粒度错了表就废了 |
| RequirementEntity（需求实体） | 一个独立的需求单，比如"建一张 DM 模型"、"定义一个指标口径" |
| binding（绑定） | 这个数从哪张表来、经过什么加工算出来的"路径记录" |
| fingerprint（指纹） | 给设计内容算的哈希值。内容改一个字，指纹就变。防止"偷偷改了还说是原来那份" |
| GATE（门禁） | 流水线上的质检关卡。过关→下一站；小毛病→打回重做；大问题→举手叫人 |
| HumanInterventionTask（人工介入） | AI 拿不准时**停下来问你**，绝不硬编。你答复后它接着干 |
| 固定点（fixed point） | 改一轮、查一轮、再改一轮……直到**连续两轮没有任何变化**，才算"彻底改完了"，才允许出最终成果 |
| 依赖矩阵 | 层与层之间的"交通法规"，白名单制：法规没写的路，一律不准走 |
| 资产决策（reuse-as-is 等） | 动手建表前先查家底：这表是不是已经有了？能直接用 / 改改用 / 还真得新建 / 查不清楚先问人 |
| CsvBundle | 所有数据装进一个文件夹：带清单（manifest，记录每个文件的行数、校验和）和封条（checksum），封条对不上就拒绝读取 |
| 三阶段状态 stale | "作废"：上游变了，下游基于旧版本做的所有东西全部失效重来 |

---

## 为什么强制全用 CSV 存数据？

原文档花大量篇幅规定"一切数据用 CSV、要有清单、要有校验和"，原因很实在：

1. **谁都能打开看**——不是私有格式，没有黑盒，审计的人不用装特殊软件。
2. **改一下就会被发现**——每个文件都记录行数和校验和，被偷偷动过马上露馅。
3. **AI 的输出可以被机器复核**——AI 说的每个结论都落在表格里，脚本能逐条重新验算，防止 AI 一本正经地胡说八道（幻觉）。
4. **复杂结构拆平存**——一对多的关系就多建一张关联表，绝不在一个单元格里塞 JSON 糊弄。

---

## 一次完整流程走一遍（贷款月报故事版）

1. **提需求**：需求方说"我要产品维度和机构维度的月末贷款余额"。
2. **拆解**：工具拆出两个粒度的需求——"月份×机构×产品"和"月份×机构"，各自登记成能力项。注意：这是**两个独立需求**，机构余额直接从产品粒度表 GROUP BY 汇总，不建立互相依赖。
3. **查家底**：先查资产库，已有的表能直接用吗？查不到确凿证据就先标记"待核实"，绝不武断说"没有，新建"。
4. **反向规划**：DM 缺表 → DWM 缺汇总 → DWD 缺明细，一层层倒推，每一跳都要过依赖矩阵检查。
5. **三阶段设计**：对每个要新建/扩展的模型，走概念 → 逻辑 → 物理，每个阶段过各自的质检（C-GATE / L-GATE / P-GATE）。
6. **正向验证**：从 DWD 往上验：机构余额 = SUM(产品余额)？维度齐吗？时间口径对吗？能追溯吗？
7. **等人确认**：生成"待确认"的设计文档。人点"确认"才有效；说"要改"就打回第 4 步重来。
8. **收敛出活**：改一轮查一轮，连续两轮没变化 → 生成最终设计文档 + PostgreSQL 建表 SQL（自动带表和字段的中文注释）。

全程任何一步卡壳（证据不足、撞了红线），工具就**停下来出人工介入任务**，问清楚了再继续，不会自作主张。

---

## 模型设计版的边界：管什么，不管什么

| ✅ 管 | ❌ 不管（已移出边界） |
|---|---|
| DWD/DWM/DM 模型的三阶段设计 | 报表长什么样（布局、筛选、分页） |
| 指标、标签的口径如何落成模型 | 数据文件、API、推送这些应用怎么交付 |
| 粒度、维度、度量、键、约束设计 | 指标/标签的对外发布和推送 |
| 物理模型确认后生成 DDL | 直接修改生产数据库、跑生产作业 |

一句话：**只管"把模型设计出来并画好建表 SQL"，不管"应用怎么用、生产怎么跑"。**

---

## 它靠什么保证"不瞎编"？

1. **不虚构资产**：查不到证据就标"待核实"或问人，绝不把"我猜没有"当成"确认新建"。
2. **一切有指纹**：设计、证据、确认全部带校验和，事后可复算、可审计。
3. **每步有门禁**：命名、粒度、依赖、说明完整性……层层关卡，不过就打回。
4. **拿不准问人**：人工介入是流程的一部分，不是异常。人的答复也会被记录和版本化。
5. **人也不能破规**：就算人工说"就这么干"，也绕不过依赖矩阵——想改规则？走正式的新版本规则流程，留痕。

---

## 初始化要准备哪些资料？

想把这个 Skill 跑起来，资料分三档：**不准备就开不了工的**、**一期就该备齐的**、**可以后补但会有代价的**。

### 🔴 第一档：阻断级——没有这些，门都进不去

| 资料 | 是什么 | 缺了会怎样 |
|---|---|---|
| 数据字典四件套（bootstrap） | `type_dictionary.csv`（类型）、`type_values.csv`（枚举值）、`field_dictionary.csv`（字段）、`dataset_keys.csv`（主键/排序键）；每个字段一行、都带中文说明 | 这是整套系统的**信任根**。没有它，任何 CSV 数据包都读不进来 |
| CSV 总纲文档 | `references/contracts/csv-schema.md`：把工作流里 40 多张 CSV 的每个字段逐一定义清楚 | 原文档明确：这份没写完之前，Skill 停留在设计阶段，**不许开始写处理器** |
| 两个校验脚本 | `validate_csv_bundle.py`（验数据包）+ `validate_csv_control_plane.py`（验提交账本）；校验器里还要"焊死"bootstrap 允许的版本号和校验和组合 | 没有裁判，清单、封条、指纹全是摆设 |
| 依赖矩阵 | `dependency_matrix.csv` + `matrix_conditions.csv`：DWD/DWM/DM 之间"谁能引用谁"的白名单（允许/禁止/有条件允许） | 没写进矩阵的依赖边一律非法——相当于交通法没颁布，所有车都不许上路 |

### 🟡 第二档：一期标配——建模质量的下限

| 资料 | 是什么 | 缺了会怎样 |
|---|---|---|
| 知识库两件套 | 业务术语表 + 词根表（版本化 CSV Bundle） | 术语归一和命名辅助变差，同一个东西被叫成好几个名字 |
| 策略规则包 | 命名、主题域、分层 四套规则 CSV（Markdown 只做解释，CSV 才是执法依据） | 对应门禁要么形同虚设，要么动不动卡住 |
| PostgreSQL 引擎 Profile | 三张表：Profile 声明（`engine_profile_metadata.csv`）、类型映射（`engine_type_mappings.csv`，逻辑类型 → PG 原生类型）、方言规则（`engine_dialect_rules.csv`，注释/索引/分区怎么写） | 物理设计落不了地，DDL 出不来。将来换数据库就要另做一套 Profile |
| 交付物模板 | 待确认模型、最终设计、检查报告的 Markdown 模板 | 输出格式不统一，没法比价 |
| 确认人名单 | 谁有权确认概念/逻辑/物理阶段产物、谁签物理模型确认、各自授权范围 | 流程走到"等确认"就死等，或被无权限的人误确认 |
| 外置工作目录 | 一个用户授权的目录；所有运行产物（RunState、证据、DDL、日志）**只能写这里**，严禁写进 Skill 自身目录 | 不给目录没法跑；production 模式还会做路径校验，写错地方直接拒绝 |

### 🟢 第三档：可以后补——但代价是"AI 频繁举手"

| 资料 | 是什么 | 缺了会怎样 |
|---|---|---|
| 资产目录（存量家底） | 现有 DWD/DWM/DM 模型清单：`catalog_manifest.csv` + `assets.csv` + `asset_fields.csv` + `asset_dependencies.csv`，含表、字段、依赖 | 没有 → 登记"空快照"，工具不敢判断"是不是已有"，只能反复人工介入问人；已有表可能被重复建设 |
| 存量模型的三阶段设计文档 | 概念/逻辑/物理的历史设计产物 | 特别注意：**光有物理表存在 ≠ 能复用**。reuse-as-is 必须验证三阶段证据，缺证据照样触发人工介入 |
| 优秀示例/反例库 | `references/examples/` 里按业务场景整理的好设计和坏设计 | AI 缺少参考样本，设计质量靠抽卡 |
| 评估场景集 | `evals/` 里真实的触发场景和预期结果 | 没法客观评估 Skill 干得好不好 |
| 分层专属设计理念 | DWD/DWM/DM 各自的物理设计细则（当前是 reserved 占位状态） | 物理确认和 DDL 会被 `awaiting-layer-implementation` 拦住——流程能走到门口，出不了 SQL |

### 📋 所有资料的通用硬性要求

不管哪一档，凡是喂给系统的资料都要遵守四条铁律：

1. **全用 CSV**：UTF-8 编码、无 BOM、LF 换行、逗号分隔，布尔值只写 `true/false`；
2. **带封条**：每个文件登记进 manifest（文件名、行数、校验和），打包成 Bundle；校验和对不上直接拒收；
3. **每个字段、每个枚举值都要有中文说明**——"没说明 = 不存在"；
4. **版本化**：所有 Bundle 登记版本号；运行开始时锁定版本，中途版本一变，之前基于旧版做的设计全部作废重来。

一句话总结：**备齐第一档（字典 + 矩阵 + 校验器），Skill 才算"能开机"；补上第二档（知识 + 规则 + 引擎配置 + 确认人），建模质量才有下限；第三档（家底 + 示例）越厚，AI 举手问人的次数越少。**

---

## 常见疑问 FAQ

**Q：为什么搞这么繁琐？**
A：银行场景。模型错误下游成片爆炸，追溯成本极高。这套流程用 upfront 的严谨换事后的安心——每个结论都能回答"依据是什么、谁确认的、基于哪个版本"。

**Q：最终交付物是什么？**
A：Markdown 格式的模型设计文档（人看的）+ 物理模型确认后的 DDL 建表 SQL（机器执行的，默认 PostgreSQL）。

**Q：它会动我的数据库吗？**
A：不会。它只输出设计和 SQL 文件，**绝不直接连生产库执行任何操作**。

**Q：AI 中途出错了怎么办？**
A：所有状态都在 CSV 里落了盘，每一步有指纹和日志。可以定位到具体哪一轮、哪个工作项出错，修复后从断点重新求解，不会整锅倒掉。

**Q：跟直接问 AI"帮我设计张表"有什么区别？**
A：直接问 AI，它可能编一个不存在的表名、编一个没依据的口径，而且下次问同样的问题答案还不一样。这个工具把建模变成**可检查的流水线**：每条结论有出处、每个字段有说明、每次改动有指纹，错了能定位，对了能复现。
